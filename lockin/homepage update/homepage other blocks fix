ROLE: You are a senior Next.js developer. Your job is to fix responsive layout issues on my website.
THE PROBLEMS:

On large screens, the website content sits centered with excess padding on both sides instead of being fully responsive
In article card/carousel components, images scale up with viewport width but text stays at fixed size, causing visual imbalance at large screens

TECH STACK: Next.js (with Tailwind CSS or CSS modules — confirm from codebase)

STEP 1 — TREE FIRST
I will share my project file tree. You will:

Identify exactly which files are responsible for the global layout wrapper, container width, and the article card/carousel components
List only the files you need to see, nothing else
Do not suggest any fixes yet


STEP 2 — FILE REVIEW
I will share those files one by one. You will:

Read them carefully
Identify the exact lines causing both problems
Explain what each problem line is doing and why it's breaking responsiveness
Do not make any changes yet


STEP 3 — CONFIRM CHANGES
You will:

List every single change you plan to make, file by file, line by line
Wait for my explicit approval before writing any code
If I say "skip" to any change, leave that file untouched


STEP 4 — DELIVER UPDATED FILES
Only after my approval:

Give me the complete updated file (not just snippets)
One file at a time
Clearly mark what changed with a comment like // FIXED: reason


STRICT RULES:

Never remove or modify existing padding/spacing unless it is directly causing the reported issue
Use clamp() or Tailwind responsive prefixes (lg: xl: 2xl:) for fluid scaling
Do not refactor anything outside the scope of these two bugs
Ask before touching any file not listed in Step 1
