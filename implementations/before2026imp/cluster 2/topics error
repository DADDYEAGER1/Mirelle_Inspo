[
  {
    "path": "src/app/posts/page.tsx",
    "content": "// src/app/posts/page.tsx\nimport { getAllCategories, getPaginatedPosts, getPostsByCategory } from '@/lib/posts';\nimport PostHero from '@/components/Posts/PostHero';\nimport CategoryNav from '@/components/Posts/CategoryNav';\nimport PostGrid from '@/components/Posts/PostGrid';\nimport Pagination from '@/components/Posts/Pagination';\nimport AdSpace from '@/components/Posts/AdSpace';\nimport type { Metadata } from 'next';\n\nexport const metadata: Metadata = {\n  title: 'Inspo Posts | Mirell\u00e9',\n  description: 'Curated beauty and nail art inspiration from Mirell\u00e9.',\n  openGraph: {\n    title: 'Inspo Posts | Mirell\u00e9',\n    description: 'Curated beauty and nail art inspiration.',\n    type: 'website',\n  },\n};\n\ninterface PostsPageProps {\n  searchParams: {\n    page?: string;\n    category?: string;\n  };\n}\n\nexport default async function PostsPage({ searchParams }: PostsPageProps) {\n  const page = parseInt(searchParams.page || '1', 10);\n  const categorySlug = searchParams.category;\n\n  // Get all categories for nav\n  const categories = await getAllCategories();\n\n  // Get posts (filtered or paginated)\n  let posts;\n  let paginationData;\n\n  if (categorySlug) {\n    // Get category posts\n    const categoryPosts = await getPostsByCategory(categorySlug);\n    const totalPosts = categoryPosts.length;\n    const perPage = 12;\n    const startIndex = (page - 1) * perPage;\n    const endIndex = startIndex + perPage;\n\n    posts = categoryPosts.slice(startIndex, endIndex);\n    paginationData = {\n      currentPage: page,\n      totalPages: Math.ceil(totalPosts / perPage),\n      hasNextPage: endIndex < totalPosts,\n      hasPreviousPage: page > 1,\n    };\n  } else {\n    // Get all posts paginated\n    const data = await getPaginatedPosts(page, 12);\n    posts = data.posts;\n    paginationData = {\n      currentPage: data.currentPage,\n      totalPages: data.totalPages,\n      hasNextPage: data.hasNextPage,\n      hasPreviousPage: data.hasPreviousPage,\n    };\n  }\n\n  return (\n    <div className=\"min-h-screen bg-background\">\n      {/* Hero */}\n      <PostHero />\n\n      {/* Main Content with Sidebar */}\n      <div className=\"max-w-[1400px] mx-auto px-3 md:px-3\">\n        <div className=\"flex flex-col md:flex-row gap-8\">\n          {/* Left: Posts (60%) */}\n          <div className=\"w-full md:w-[60%]\">\n            {/* Category Navigation */}\n            <CategoryNav categories={categories} />\n\n            {/* Post Grid */}\n            <PostGrid posts={posts} />\n\n            {/* Pagination */}\n            <Pagination\n              currentPage={paginationData.currentPage}\n              totalPages={paginationData.totalPages}\n              hasNextPage={paginationData.hasNextPage}\n              hasPreviousPage={paginationData.hasPreviousPage}\n              category={categorySlug}\n            />\n          </div>\n\n          {/* Right: Ad Space (40%) - Hidden on mobile */}\n          <div className=\"hidden md:block md:w-[40%]\">\n            <AdSpace />\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n}"
  },
  {
    "path": "src/lib/posts.ts",
    "content": "// src/lib/posts.ts\nimport fs from 'fs';\nimport path from 'path';\nimport matter from 'gray-matter';\nimport { remark } from 'remark';\nimport html from 'remark-html';\nimport { Post, PostMetadata, Category } from '@/types/posts';\n\nconst postsDirectory = path.join(process.cwd(), 'src/content/posts');\n\n// Get all post slugs\nexport async function getAllPostSlugs(): Promise<string[]> {\n  if (!fs.existsSync(postsDirectory)) {\n    return [];\n  }\n  const fileNames = fs.readdirSync(postsDirectory);\n  return fileNames\n    .filter(fileName => fileName.endsWith('.md'))\n    .map(fileName => fileName.replace(/\\.md$/, ''));\n}\n\n// Get single post by slug\nexport async function getPost(slug: string): Promise<Post | null> {\n  try {\n    const fullPath = path.join(postsDirectory, `${slug}.md`);\n    \n    if (!fs.existsSync(fullPath)) {\n      return null;\n    }\n\n    const fileContents = fs.readFileSync(fullPath, 'utf8');\n    const { data, content } = matter(fileContents);\n\n    // Convert markdown to HTML\n    const processedContent = await remark().use(html).process(content);\n    const contentHtml = processedContent.toString();\n\n    return {\n      slug,\n      title: data.title || '',\n      excerpt: data.excerpt || '',\n      date: data.date || new Date().toISOString(),\n      author: data.author || 'Mirell\u00e9',\n      category: data.category || 'UNCATEGORIZED',\n      image: data.image || '/images/default-post.jpg',\n      imageAlt: data.imageAlt,\n      imageWidth: data.imageWidth,\n      imageHeight: data.imageHeight,\n      carouselImages: data.carouselImages,\n      products: data.products,\n      readTime: data.readTime,\n      canonical: data.canonical || `https://mirelleinspo.com/posts/${slug}`,\n      dateModified: data.dateModified,\n      content: contentHtml,\n    };\n  } catch (error) {\n    console.error(`Error loading post ${slug}:`, error);\n    return null;\n  }\n}\n\n// Get all posts\nexport async function getAllPosts(): Promise<PostMetadata[]> {\n  const slugs = await getAllPostSlugs();\n  const posts = await Promise.all(\n    slugs.map(async (slug) => {\n      const post = await getPost(slug);\n      if (!post) return null;\n      const { content, ...metadata } = post;\n      return metadata;\n    })\n  );\n\n  return posts\n    .filter((post): post is PostMetadata => post !== null)\n    .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());\n}\n\n// Get posts by category\nexport async function getPostsByCategory(category: string): Promise<PostMetadata[]> {\n  const allPosts = await getAllPosts();\n  return allPosts.filter(post => \n    post.category.toLowerCase() === category.toLowerCase()\n  );\n}\n\n// Get all categories (auto-generated from posts)\nexport async function getAllCategories(): Promise<Category[]> {\n  const allPosts = await getAllPosts();\n  const categoryMap = new Map<string, number>();\n\n  allPosts.forEach(post => {\n    const category = post.category;\n    categoryMap.set(category, (categoryMap.get(category) || 0) + 1);\n  });\n\n  return Array.from(categoryMap.entries())\n    .map(([name, count]) => ({\n      name,\n      slug: name.toLowerCase().replace(/\\s+/g, '-'),\n      count,\n    }))\n    .sort((a, b) => a.name.localeCompare(b.name));\n}\n\n// Get related posts (same category, then recent)\nexport async function getRelatedPosts(currentSlug: string, limit: number = 3): Promise<PostMetadata[]> {\n  const currentPost = await getPost(currentSlug);\n  if (!currentPost) return [];\n\n  const allPosts = await getAllPosts();\n  \n  // Filter out current post\n  const otherPosts = allPosts.filter(post => post.slug !== currentSlug);\n\n  // Same category posts first\n  const sameCategoryPosts = otherPosts.filter(\n    post => post.category === currentPost.category\n  );\n\n  // If not enough same category posts, add recent posts\n  const relatedPosts = [\n    ...sameCategoryPosts.slice(0, limit),\n    ...otherPosts.filter(post => !sameCategoryPosts.includes(post)).slice(0, limit - sameCategoryPosts.length)\n  ];\n\n  return relatedPosts.slice(0, limit);\n}\n\n// Pagination helper\nexport async function getPaginatedPosts(page: number = 1, perPage: number = 12) {\n  const allPosts = await getAllPosts();\n  const totalPosts = allPosts.length;\n  const totalPages = Math.ceil(totalPosts / perPage);\n  const startIndex = (page - 1) * perPage;\n  const endIndex = startIndex + perPage;\n\n  return {\n    posts: allPosts.slice(startIndex, endIndex),\n    currentPage: page,\n    totalPages,\n    totalPosts,\n    hasNextPage: page < totalPages,\n    hasPreviousPage: page > 1,\n  };\n}"
  },
  {
    "path": "src/content/posts/test.md",
    "content": "\n```yaml\n---\ntitle: \"The Ultimate Guide to Glass Nails\"\ndate: \"2024-01-15\"\nauthor: \"Sarah Mitchell\"\ncategory: \"NAIL ART\"\nexcerpt: \"Discover the trending glass nail technique that's taking over Instagram. Learn how to achieve this stunning mirror-like finish at home.\"\nimage: \"/images/posts/glass-nails.jpg\"\nimageAlt: \"Close-up of glass nail technique showing mirror finish\"\nimageWidth: 1200\nimageHeight: 1200\ncarouselImages:\n  - url: \"/images/posts/glass-nails-step1.jpg\"\n    alt: \"Step 1: Base coat application\"\n  - url: \"/images/posts/glass-nails-step2.jpg\"\n    alt: \"Step 2: Chrome powder application\"\n  - url: \"/images/posts/glass-nails-step3.jpg\"\n    alt: \"Step 3: Final buffing\"\nproducts:\n  - image: \"/images/products/chrome-powder.jpg\"\n    title: \"Professional Chrome Powder\"\n    price: \"$24.99\"\n    brand: \"BEAUTY PRO\"\n    url: \"https://example.com/product\"\n  - image: \"/images/products/base-coat.jpg\"\n    title: \"No-Wipe Base Coat\"\n    price: \"$15.99\"\n    brand: \"NAIL STUDIO\"\n    url: \"https://example.com/product2\"\nreadTime: \"8 min\"\ncanonical: \"https://mirelleinspo.com/posts/glass-nail-technique\"\ndateModified: \"2024-01-20\"\n---\n```\n\n---\n\n## Content Guidelines\n\n**Length:** 800-1000 words (keep it concise)\n\n**Structure:**\n1. Opening paragraph (hook)\n2. Main content (3-4 paragraphs)\n3. Tips/techniques (2-3 paragraphs)\n4. Closing thoughts\n\n**Styling:**\n- Use `##` for section headings\n- Use `**bold**` for emphasis\n- Keep paragraphs 3-4 sentences\n- Natural, conversational tone\n\n---\n\n## Full Example Post\n\n```markdown\n---\ntitle: \"5 Minimalist Nail Designs You'll Love\"\ndate: \"2024-02-01\"\nauthor: \"Emma Chen\"\ncategory: \"TRENDS\"\nexcerpt: \"Minimalist nails are having a moment. Here are 5 chic designs that prove less is more.\"\nimage: \"/images/posts/minimalist-nails.jpg\"\nimageAlt: \"Minimalist nail art featuring clean lines\"\nimageWidth: 1200\nimageHeight: 1200\nproducts:\n  - image: \"/images/products/nude-polish.jpg\"\n    title: \"Nude Perfection Polish\"\n    price: \"$12.99\"\n    brand: \"ESSIE\"\n    url: \"https://example.com/nude-polish\"\nreadTime: \"6 min\"\n---\n\nMinimalist nail art has taken the beauty world by storm, and for good reason. These understated designs offer elegance without overwhelming your look. Whether you're heading to the office or a special event, minimalist nails work for every occasion.\n\n## Why Minimalist Nails Are Trending\n\nIn a world of maximalist designs and bold colors, there's something refreshing about simplicity. Minimalist nails let your natural beauty shine through while adding just enough detail to make a statement. They're also incredibly versatile and easy to maintain.\n\n## The Classic French Minimalist\n\nThe French manicure gets a modern update with ultra-thin white tips. This sleek version uses barely-there lines that elongate your fingers and complement any outfit. It's perfect for those who want polish without the fuss.\n\n## Single Line Accents\n\nOne thin line down the center of each nail creates visual interest without overwhelming the design. Choose metallic gold or silver for a touch of luxury, or stick with black for a bold contrast against nude nails.\n\n## Negative Space Magic\n\nNegative space designs use the natural nail as part of the art. Geometric shapes, half-moon crescents, or simple cutouts create stunning effects that are surprisingly easy to achieve at home.\n\n## Tiny Dot Perfection\n\nA single dot placed strategically on each nail adds personality without commitment. Try placing dots near the cuticle, at the tip, or clustered on one accent nail for maximum impact.\n\n## Subtle Gradient Fade\n\nA soft ombre from nude to white creates dimension while maintaining that minimalist aesthetic. This design works beautifully for both short and long nails.\n\nThe beauty of minimalist nail art lies in its simplicity. These designs prove you don't need elaborate patterns or bright colors to make your nails stand out. Start with one of these five looks and discover how less truly can be more.\n```\n\n---\n\n## Categories to Use\n\nUse ONE of these categories (exact spelling):\n- NAIL ART\n- TRENDS\n- BEAUTY TIPS\n- WELLNESS\n- LIFESTYLE\n- TECHNIQUES\n- PRODUCTS\n\nCategories auto-populate in the navigation bar.\n\n---\n\n## Image Guidelines\n\n**Main Image:**\n- Square ratio (1200x1200 recommended)\n- Save in: `/public/images/posts/`\n- Use descriptive filenames\n\n**Carousel Images (optional):**\n- Add 2-5 images for step-by-step guides\n- Same square ratio\n\n**Product Images (optional):**\n- Add 2-4 products if relevant\n- Include affiliate URLs\n\n---\n\n## Checklist Before Publishing\n\n- [ ] Frontmatter complete with all required fields\n- [ ] Content is 800-1000 words\n- [ ] Images uploaded to correct folder\n- [ ] Category matches one from list\n- [ ] Excerpt is compelling (140-160 characters)\n- [ ] Date format: \"YYYY-MM-DD\"\n- [ ] Author name correct\n- [ ] Products have working URLs (if included)\n\n---\n\n## File Naming Convention\n\nUse lowercase with hyphens:\n- \u2705 `glass-nail-technique.md`\n- \u2705 `minimalist-designs-2024.md`\n- \u274c `Glass Nail Technique.md`\n- \u274c `minimalist_designs.md`\n\n---\n\n## Testing Your Post\n\n1. Save file in `src/content/posts/`\n2. Restart dev server\n3. Visit `/posts` to see it in the grid\n4. Visit `/posts/your-slug` to see full post\n5. Check that category filter works\n6. Verify related posts appear at bottom\n\n---\n\n## Need Help?\n\nCommon issues:\n- **Post not showing:** Check frontmatter syntax (YAML formatting)\n- **Image broken:** Verify path starts with `/images/posts/`\n- **Category not appearing:** Make sure it matches exactly\n- **Content not rendering:** Check for markdown syntax errors"
  }
]
